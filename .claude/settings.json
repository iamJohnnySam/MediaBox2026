{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && (git diff --cached --quiet || git commit -m \"Auto-commit after prompt\") && git push origin HEAD:master"
          }
        ]
      }
    ]
  }
}
